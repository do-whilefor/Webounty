# ---- Python ----
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# ---- webounty 运行期产物 ----
# 会话 Wiki、原始观察与证据、operations.jsonl 日志都写在 .webounty/<会话标识>/ 下。
# 这些属于具体测试过程，不进入仓库。
.webounty/

# 全局约束文件使用的黑板状态（若在本仓库内运行）
state/

# 显式导出的快照（finish --export），默认落在会话目录之外；
# 导出时应指向仓库外目录，如确实落在仓库内请按实际名称补充忽略规则。

# ---- 基准 / 回放产生的临时输出 ----
*.heapsnapshot
trace.json.gz
*.trace.json
